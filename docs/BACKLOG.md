# token-dashboard — Backlog

Open work organized by horizon (Now / Soon / Someday) with `[tag]` for
domain. Markdown checkboxes; edit by hand. Shipped items go to
`CHANGELOG.md`, not here.

## Now

Items we're actively working on or planning to do imminently.
_Nothing active — `node scripts/reconcile.mjs` agrees with ccusage on all 11 models as of 2026-07-28._
- [x] **[bug]** **SHIPPED 2026-08-14 — both halves, see CHANGELOG.** Decision: rates scoped to the window, totals left whole and captioned. Trimming the headline totals would have discarded $4.08 of real spend to fix what is a labelling problem, so `coverageWindow()` scopes only the quotient (burn rate, projection) and a caption under MetricsStrip says the totals are the sum of what survives rather than a lifetime figure. The rate error was larger than expected: **$78.49/day over a padded 110-day span, against $132.77/day over the real 65 — 41% understated.** Original item: **Cost history before ~2026-06-11 is permanently lost, and every total in this dashboard understates because of it.** Claude Code's `cleanupPeriodDays` defaults to 30 and was not set until 2026-07-27 (confirmed: the May 28 settings backup has no such key), so transcripts were being deleted continuously until then. `~/.claude/history.jsonl` survived the sweep and records 471 prompts in March, 398 in April, 467 in May — 1,336 total, MORE than June (358) + July (662) combined — while tokens.json holds $0 for all of it. The trend panels now trim to the coverage boundary and say so (CoverageNote), but the headline totals, MetricsStrip, burn rate and projections still silently treat the missing months as real zeros. Decide what to do: either scope the headline figures to the coverage window explicitly, or surface a 'data since <date>' qualifier globally. Note the prompts are recoverable but their TOKEN COUNTS ARE NOT — history.jsonl carries no usage, so the spend for that period can never be reconstructed from local data. <!-- added 2026-08-05 --> <!-- added 2026-08-05 -->
- [ ] **[tech-debt]** expectedShift accepts a bare dimension that can never match, silently. After the final-review fix, isPreRegistered matches "dimension/category" or a bare CATEGORY, so a declaration of expectedShift: ['model'] — the natural thing to write, and what the spec's prose invites — now exempts nothing and the user gets an unexplained confounded verdict where they had pre-registered. readInterventions CAN catch this: it cannot know category names (those live in the data) but the dimension names are a fixed closed list already hardcoded in intervention.js, and this module already restates exactly that kind of closed list (FACTOR_KEYS) for exactly that kind of validation. Fix is ~6 lines: warn (do not skip, the entry is inert anyway) when a bare expectedShift entry is 'model', 'project' or 'tokenType'. Exclude 'subagent' — it is legitimately both a dimension and a category. Deferred from the analysis-layer branch because the process allows one fix wave and this failure is honest rather than misleading: the shift still renders, only without the pre-registered label. Blast radius today is zero (no interventions.json on this machine). <!-- added 2026-09-04 -->

## Soon

Items we want to tackle in the near term but aren't started yet.
- [x] **[feat]** **SHIPPED 2026-09-04 — driver decomposition (5-factor cost identity) and intervention evaluation (pre-registered `expect`, confound detection, frozen results) both landed; see CHANGELOG.** Real-corpus verification at ship time: 32.42 requests/turn, agreeing with the 32.81 measured at the 2026-09-03 design pass, subagent turns exactly 0, zero sessions breaching the requests/turn >= 1 floor. Original item: **Analysis layer** — driver decomposition + intervention measurement, designed 2026-09-03, spec at `docs/superpowers/specs/2026-09-03-analysis-layer-design.md`. First of three layers toward a work-usable copy (Outcome and Presentation follow). Sequenced first because it is the only one needing elapsed time: a before/after is worthless without a baseline predating the intervention. Blocked on nothing; needs an implementation plan. Two findings from the design pass worth keeping even if the build slips: the parser has **no turn count at all**, and counting `type === 'user'` naively overcounts 11.6x (2,112 vs 182 over 40 transcripts) because tool results are logged as user entries — the giveaway is requests/turn landing at 0.72, below the structural floor of 1. Real figure is **8.41 requests/turn**. `last-prompt` is NOT a usable turn proxy (matched 0 of 40 files). Second: an early draft assigned sessions whole to their start date and had to be corrected to day-slicing — 85 of 2,729 sessions are multi-day but carry **76.9% of cost**, the same trap the per-call panel hit in 2026-08-03. <!-- added 2026-09-03 -->
- [ ] **[perf]** Daily rollups + bounded session list in tokens.json — deferred deliberately 2026-07-26 after measurement: payload is 1.44MB/2391 sessions growing ~600KB/day, but only 109 sessions are >30d, so age-based archiving would drop 4.5% of rows and fix nothing. Transfer cost is already solved (no cache-buster => 304 on unchanged; gzip plugin => 147KB when changed). What remains is browser JSON.parse of the full payload on each change, binding somewhere north of ~10MB (weeks out). Design when needed: ingest emits rollups.daily[] over ALL sessions + a bounded sessions[] (recent window + top-N by cost) + a coverage{} block; aggregates (UsageChart, ActivityHeatmap, Cost/Model/Tool/Project breakdowns, SummaryCards) read rollups, SessionsTable/ExpensiveSessions read the bounded list, older detail lazy-loads from a second file. Do NOT truncate sessions[] before moving those aggregates — they would silently understate. <!-- added 2026-07-26 -->
- [ ] **[feat]** Burn-rate + 5-hour block panel, and a cache-efficiency panel — the natural follow-ons once the status line proves out. Claude Code's statusLine payload already carries rate_limits.five_hour.{used_percentage,resets_at}, so the block data needs no new parsing. Cache efficiency matters because 88% of spend is cache traffic (60% read / 28% write): a per-project read:write ratio answers 'are long sessions amortising their cache writes or just re-reading?'. Hold both until there is evidence the status line changed behaviour — if it did not, more instrumentation will not help and the answer is structural (shorter sessions, more delegation). <!-- added 2026-07-27 --> **Partly superseded 2026-08-03:** the cache-efficiency half shipped as the Cost mix over time panel, on a different justification — that one is retrospective (is the share falling?) rather than another live nudge, so the hold-for-evidence argument did not bind. The burn-rate / 5-hour block panel is still held.
- [x] **[feat]** Phase B — Per API call panel. **SHIPPED 2026-08-03**, see CHANGELOG. Landed larger than specced: a design review found the per-call trend could not be bucketed on `startedAt` at all (95.2% of Claude main-thread calls live in transcripts spanning >1 calendar day, max 19.2 days), so parsers now emit per-day slices and Phase A's panel was retrofitted onto them too. Two spec assumptions were wrong and are corrected in `docs/superpowers/specs/2026-08-03-per-call-context-design.md`: the main-vs-subagent split needed no second field beyond an explicit flag (the `claude_sub_` id prefix already carried it), and the proposed "tokens but zero apiCalls" diagnostic was structurally dead.
- [ ] [tech-debt] test/glossary.test.js only scans literal term="..." props, so any dynamic term={expr} silently opts out of the typo guard. Both new analysis panels carry colocated TERMS guards as a workaround; a general fix would make the contract real repo-wide. <!-- added 2026-09-03 -->
- [ ] **[feat]** Spec guard 5 (Variance) was never built — the intervention panel shows two bare averages. The spec at docs/superpowers/specs/2026-09-03-analysis-layer-design.md requires the daily series for BOTH sides with n, mean and median, and calls the median load-bearing rather than decorative: daily cost has a coefficient of variation of 1.10, median $71 against mean $113, so the mean alone is dragged by a handful of expensive days. A 20% drop inside 50% daily spread is not a finding, and the panel currently cannot show that it isn't one. Nothing in factorsFor, evaluate or InterventionPanel computes a median or a per-day series. Deliberately deferred out of the analysis-layer build (2026-09-04 fix wave) because a per-window daily distribution is a feature, not a fix, and the wave already carried a Critical — but deferred VISIBLY: the plan previously claimed these figures were 'computed in factorsFor but not yet rendered', which was false and has been corrected in place. <!-- added 2026-09-04 -->

## Someday

Latent items captured for future-when-they-bite. Not blocking anything now.
- [ ] **[tech-debt]** Chart tests are inconsistent about the ResponsiveContainer mock, so half of them assert nothing. jsdom reports zero size, so recharts renders no children and any assertion about chart internals passes vacuously. `CostMixTrend.test.jsx` mocks ResponsiveContainer to a fixed 800x300 and therefore exercises real recharts output; `UsageChart.test.jsx` does not, so its two chart assertions are green regardless. Now that one file demonstrates the pattern, the other reads as a trap. Apply the same mock to UsageChart (and any future chart test) and make its assertions real. <!-- added 2026-08-03 -->
- [ ] **[tech-debt]** Pin the Share-mode y-axis ticks. The domain is fixed `[0, 100]`, but recharts picks the tick set itself and it drifts with the data — observed settling to an uneven `0/30/60/100` (a 40-point gap at the top) where it had been `0/25/50/75/100`. Passing `ticks={[0, 25, 50, 75, 100]}` makes it deterministic. Cosmetic only. Note the related root cause is already fixed: the Share tick formatter had no rounding where the Dollars branch did, which is why a raw `100.0000001%` reached the browser. <!-- added 2026-08-03 -->
- [ ] **[tech-debt]** Two small test gaps from the cost-mix review, both deliberate deferrals: `formatAxisDollars` (extracted to `lib/format.js` so UsageChart and CostMixTrend format the same dollars axis identically) has no direct unit test — it was verified once against the old inline formatter over 200k random values, but nothing guards it going forward; and the ToggleButton style test asserts the active and inactive backgrounds *differ* without asserting which maps to which, so swapping the branches would still pass. <!-- added 2026-08-03 -->

<!-- Suggested tags: bug, feat, perf, tech-debt, docs, ops, security, strategic. Use whatever fits. -->
- [ ] **[feat]** Subagent context is 32.1% of weighted spend and the boundary detector cannot reach it — UserPromptSubmit never fires inside a subagent. Investigate a PreToolUse matcher on Task to scope/cap subagent prompts, or SubagentStop accounting to at least measure it. Note most of this spend is GOOD (keeps bulk out of main context); the lever is fewer/better-scoped dispatches, not less delegation. <!-- added 2026-08-03 -->
- [ ] **[perf]** Phase 3 of boundary detector: reduce the 35.6k session floor. It is 88% of every boundary's cost, so cutting it multiplies the detector's value (35.6k->25k improves break-even at 200k from 3.1 to 2.1 turns). Largest addressable chunk is 82 SKILL.md files whose frontmatter loads every session (~5-8k tok); CLAUDE.md is only 253 tok and MEMORY.md ~1990. Needs per-component instrumentation first — the decomposition is partly inferred. <!-- added 2026-08-03 -->
- [ ] **[bug]** Unverified claim worth checking: cnighswonger/claude-code-cache-fix (396 stars) reports a ~20x cache regression on RESUMED sessions from block scatter and fingerprint instability. Not acknowledged by Anthropic. A cache bug and a context-size problem look identical in aggregate totals, so confirm local cache-read ratios are not inflated by this before attributing all spend to context length. Current measured ratio is 17.7 reads per write, which looks healthy. <!-- added 2026-08-03 -->
- [ ] **[feat]** Per-tool context-window reference lines on the Per API call panel. Claude main runs against a 1M window (max observed per call 999,722) while Codex self-reports 258,400 (max 236,013), so equal heights on the shared y-axis mean very different headroom — a 250k Codex point is near its ceiling, a 250k Claude point is a quarter of the way up. A faint per-tool ceiling line would fix it. Deliberately deferred rather than crowding a four-line chart. <!-- added 2026-08-03 --> <!-- added 2026-08-03 -->
- [ ] **[tech-debt]** opencode emits a single-day 'daily' slice stamped at startedAt, carrying the whole session, so it inherits the smearing every other tool was just fixed for. Accepted because opencode is 27 sessions and $8.24 lifetime (0.1% of spend), its sessions are short, and it is not plotted on the Per API call panel — its message rows DO carry per-message tokens and time, so a real split is available if it ever becomes material. The reason not to do it now: the session row's aggregates are authoritative and rebuilding from messages risks breaking the daily-sums-to-session-totals invariant, since opencode revert/undo deletes messages while aggregates persist. <!-- added 2026-08-03 --> <!-- added 2026-08-03 -->
- [ ] **[tech-debt]** The boundary detector's backtest gate is not reproducible over time — it walks the live ~/.claude/projects/, which both gains files and grows existing transcripts in place. Observed drifting 33.8% -> 36.0% firing within one day of ordinary use at the old 275k arm, which is what forced the retune to ARM 325k / CEILING 450k (now ~7pp of headroom, needs ~9 consecutive firing sessions to flip rather than 1). Fix is to pin or snapshot the corpus so the gate measures a fixed population. IMPORTANT: the correct response to a future failure is to investigate the corpus, NOT to loosen the 35% target. <!-- added 2026-08-07 -->
- [ ] **[feat]** The spec's 'band consumption rule' was never built. The design named it as the mitigation for its own highest-severity risk — the model rationalising 'continue' indefinitely and silently disabling the detector — by having a continue verdict NOT consume the band. Shipped code consumes the band at fire time unconditionally; there is no return channel from the model. Consequently state.verdicts holds fire records, not verdicts (identical in shape whatever the model decides), and nothing reads the array. Risk is currently unmitigated and the field name is misleading. Either build the return channel or rename the field and drop the claim. <!-- added 2026-08-07 -->
- [ ] **[feat]** Use ~/.claude/history.jsonl as a direct deletion detector for the turn coverage guard. It survives the 30-day retention sweep and records one entry per prompt per day, so any day with history.jsonl prompts but zero ingested turns PROVES the transcript was deleted rather than inferring it from the coverage boundary. That would turn the coverage guard from a proxy into a measurement. A genuine improvement, but it introduces a new data source and the existing coverageWindow() floor is sufficient to close the current blocker, so deliberately deferred rather than folded into the analysis-layer build. <!-- added 2026-09-04 -->
- [ ] **[tech-debt]** Two documentation nits left by the analysis-layer final review. (1) docs/superpowers/plans/2026-09-03-analysis-layer.md:1452 still carries the defective snippet evaluate(sessions, { ...iv, expectedShift: ['model'] }) asserting toBe('supported'), unannotated, while the CORRECTION note added during the fix wave sits 87 lines later at :1539 beside compareShares — so the plan describes a test that would now fail. (2) README overstates the frozen-result sidecar: extra fields are carried through by mergeResults but the panel displays only verdict, frozenAt, reasons and confounds, so 'displayed as-is' is wrong. Both doc-only. <!-- added 2026-09-04 -->
